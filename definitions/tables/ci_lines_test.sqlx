config {
  type: "table"
}

SELECT
  *
FROM
  `prj-lii-datalake-devtest.transform_evolve.daily_evolve_customers` limit 10

  --where not exists (
 -- select 1 from ${ref("stg_tab")} where batch is null
--)