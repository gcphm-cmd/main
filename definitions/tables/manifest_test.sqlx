config {
  type: "table"
}

-- Only select data if assertion passes (no null file_no)
SELECT
  *
FROM
  ${ref("staging_manifest_test")}
WHERE NOT EXISTS (
  SELECT 1 FROM ${ref("staging_manifest_test")} WHERE salesman_cd IS NULL
)
